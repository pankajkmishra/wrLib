
void do_waveform_iteration(---input-arguments){
    if (WF_type == classical) {
    // write classical waveform relaxation algo here.
    // use 'do_one_time_step' from setupPDE class to solve subdomain problems
    // use 'send_and_receieve' from the class parcom;
        for (k=1, k<K, k++){
            if (k==1){
                Guess initial coupling condition
            }
           
        }
        
        
        
        
        
        
        
        
        
        
    }
    if (WF_type == pipeline) {
    // write classical waveform relaxation algo here.
    // use 'do_one_time_step' from setupPDE class to solve subdomain problems
    }
}
